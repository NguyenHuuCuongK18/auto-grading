<Window x:Class="SolutionGrader.UI.GradingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SolutionGrader.UI"
        mc:Ignorable="d"
        Title="Auto Grading System - Grading" 
        Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Loaded="Window_Loaded"
        Closing="Window_Closing">
    
    <Window.Resources>
        <!-- Status to Color Converter -->
        <local:StatusToColorConverter x:Key="StatusToColorConverter" />
        
        <!-- Button Style -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        
        <!-- Boolean to Visibility Converter -->
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header Section (compact) -->
        <Border Grid.Row="0" Background="#2D2D30" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Auto Grading System" 
                               FontSize="18" 
                               FontWeight="Bold" 
                               Foreground="White"
                               Margin="0,0,10,0"/>
                    <TextBlock x:Name="txtConfigInfo" 
                               Foreground="#AAAAAA"
                               FontSize="11"
                               VerticalAlignment="Center"/>
                </StackPanel>
                
                <Button Grid.Column="1" 
                        Content="← Back to Setup"
                        Background="Transparent"
                        Foreground="White"
                        BorderBrush="#666666"
                        Padding="10,6"
                        Click="BackToSetup_Click"/>
            </Grid>
        </Border>
        
        <!-- Control Section (compact without toggle) -->
        <Border Grid.Row="1" BorderBrush="#CCCCCC" BorderThickness="0,0,0,1" Padding="8" Background="#F8F9FA">
            <StackPanel>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Section 1: Student Selection (compact) -->
                    <Border Grid.Row="0" Background="White" BorderBrush="#DEE2E6" BorderThickness="1" CornerRadius="4" Padding="8">
                        <StackPanel>
                            <TextBlock Text="STUDENT SELECTION" 
                                       FontWeight="Bold"
                                       FontSize="12"
                                       Foreground="#495057"
                                       Margin="0,0,0,6"/>
                            
                            <WrapPanel ItemHeight="28" ItemWidth="Auto" VerticalAlignment="Center" Margin="0" >
                                <TextBlock Text="Quick Select by Index Range:" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="SemiBold"/>
                                <TextBlock Text="From:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                <TextBox x:Name="txtSelectStartIndex" Width="60" Height="24" VerticalContentAlignment="Center" TextAlignment="Center" FontSize="12" Text="1" ToolTip="Start index for selection (1-based)."/>
                                <TextBlock Text="To:" VerticalAlignment="Center" Margin="8,0,4,0"/>
                                <TextBox x:Name="txtSelectEndIndex" Width="60" Height="24" VerticalContentAlignment="Center" TextAlignment="Center" FontSize="12" Text="-1" ToolTip="End index (1-based, -1 for end)."/>
                                <Button x:Name="btnApplyIndexSelection" Content="✓ Apply" Background="#007BFF" Foreground="White" Padding="10,4" Margin="8,0,0,0" FontSize="12" Cursor="Hand" Click="ApplyIndexSelection_Click"/>
                                
                                <Border Width="2" Height="24" Background="#DEE2E6" Margin="10,0"/>
                                
                                <TextBlock Text="Quick Select by Paper:" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="SemiBold"/>
                                <ComboBox x:Name="cmbPaperSelection" Width="120" Height="24" VerticalContentAlignment="Center" SelectionChanged="PaperSelection_SelectionChanged"/>
                                
                                <Border Width="2" Height="24" Background="#DEE2E6" Margin="10,0"/>
                                
                                <Button x:Name="btnSelectAll" Content="☑ Select All" Background="#6C757D" Foreground="White" Padding="10,4" Margin="0,0,6,0" FontSize="12" Cursor="Hand" Click="SelectAll_Click"/>
                                <Button x:Name="btnUnselectAll" Content="☐ Unselect All" Background="#6C757D" Foreground="White" Padding="10,4" FontSize="12" Cursor="Hand" Click="UnselectAll_Click"/>
                            </WrapPanel>
                        </StackPanel>
                    </Border>
                    
                    <!-- Spacer -->
                    <Border Grid.Row="1" Background="Transparent" Height="8" />

                    <!-- Section 2: Grading Actions (compact) -->
                    <Border Grid.Row="2" Background="White" BorderBrush="#DEE2E6" BorderThickness="1" CornerRadius="4" Padding="8">
                        <WrapPanel ItemHeight="28" ItemWidth="Auto" VerticalAlignment="Center">
                            <TextBlock Text="Batch Size:" VerticalAlignment="Center" Margin="0,0,6,0" FontWeight="SemiBold" Foreground="#495057"/>
                            <TextBox x:Name="txtMaxParallelStudents" Width="50" Height="24" VerticalContentAlignment="Center" TextAlignment="Center" FontSize="12" FontWeight="Bold"/>
                            
                            <Border Width="2" Height="24" Background="#DEE2E6" Margin="10,0"/>
                            
                            <TextBlock Text="Start:" VerticalAlignment="Center" Margin="0,0,6,0" FontWeight="SemiBold" Foreground="#495057"/>
                            <Button x:Name="btnStartAll" Content="▶ Start All" Background="#28A745" Foreground="White" Padding="12,5" Margin="0,0,6,0" FontSize="12" Cursor="Hand" Click="StartAll_Click"/>
                            <Button x:Name="btnStartSelected" Content="▶ Start Selected" Background="#17A2B8" Foreground="White" Padding="12,5" Margin="0,0,6,0" FontSize="12" Cursor="Hand" Click="StartSelected_Click"/>
                            
                            <Border Width="2" Height="24" Background="#DEE2E6" Margin="10,0"/>
                            
                            <TextBlock Text="Control:" VerticalAlignment="Center" Margin="0,0,6,0" FontWeight="SemiBold" Foreground="#495057"/>
                            <Button x:Name="btnPause" Content="⏸ Pause" Background="#FFC107" Foreground="#000" Padding="12,5" Margin="0,0,6,0" FontSize="12" Cursor="Hand" Click="Pause_Click"/>
                            <Button x:Name="btnResume" Content="▶ Resume" Background="#20C997" Foreground="White" Padding="12,5" Margin="0,0,6,0" FontSize="12" Cursor="Hand" Click="Resume_Click"/>
                            
                            <Border Width="2" Height="24" Background="#DEE2E6" Margin="10,0"/>
                            
                            <TextBlock Text="Reset:" VerticalAlignment="Center" Margin="0,0,6,0" FontWeight="SemiBold" Foreground="#495057"/>
                            <Button x:Name="btnResetAll" Content="↻ Reset All" Background="#DC3545" Foreground="White" Padding="12,5" Margin="0,0,6,0" FontSize="12" Cursor="Hand" Click="ResetAll_Click"/>
                            <Button x:Name="btnResetSelected" Content="↻ Reset Selected" Background="#FD7E14" Foreground="White" Padding="12,5" FontSize="12" Cursor="Hand" Click="ResetSelected_Click"/>
                        </WrapPanel>
                    </Border>
                </Grid>
            </StackPanel>
        </Border>
        
        <!-- Main Content Area: DataGrid occupies full row -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <DataGrid Grid.Column="0" 
                      x:Name="dgStudents"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      SelectionMode="Extended"
                      AlternatingRowBackground="#F8F8F8"
                      GridLinesVisibility="Horizontal"
                      Margin="10,10,10,177" 
                      RowHeight="28"
                      EnableColumnVirtualization="True"
                      EnableRowVirtualization="True"
                      ScrollViewer.CanContentScroll="True"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      SelectionChanged="dgStudents_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Select" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          HorizontalAlignment="Center" VerticalAlignment="Center" Padding="6"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="#" Binding="{Binding Id}" Width="40" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Student Code" Binding="{Binding StudentCode}" Width="150" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Paper" Binding="{Binding PaperNo}" Width="60" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding StatusDisplay}" Width="100" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Mark" Binding="{Binding Mark, StringFormat=N1}" Width="60" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Max" Binding="{Binding MaxMark, StringFormat=N1}" Width="50" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Progress" Binding="{Binding ProgressPercent, StringFormat={}{0}%}" Width="70" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Start" Binding="{Binding StartTime, StringFormat=HH:mm:ss}" Width="70" IsReadOnly="True"/>
                    <DataGridTextColumn Header="End" Binding="{Binding EndTime, StringFormat=HH:mm:ss}" Width="70" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Duration" Binding="{Binding Duration}" Width="80" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Message" Binding="{Binding StatusMessage}" Width="*" IsReadOnly="True"/>
                </DataGrid.Columns>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Status}" Value="Success">
                                <Setter Property="Background" Value="#D4EDDA"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Status}" Value="Failed">
                                <Setter Property="Background" Value="#F8D7DA"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Status}" Value="InProgress">
                                <Setter Property="Background" Value="#FFF3CD"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Status}" Value="Paused">
                                <Setter Property="Background" Value="#D1ECF1"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Status}" Value="Not_Run">
                                <Setter Property="Background" Value="#FFFFFF"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
            </DataGrid>
        </Grid>
        
        <!-- Grading Log: horizontal below grid -->
        <Border Grid.Row="3" BorderBrush="#CCCCCC" BorderThickness="1" Margin="10,0,10,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <Border Grid.Row="0" Background="#2D2D30" Padding="10" Margin="0,-153,0,0">
                    <TextBlock Text="Grading Log" Foreground="White" FontWeight="Bold"/>
                </Border>
                
                <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <TextBox x:Name="txtLog"
                             IsReadOnly="True"
                             TextWrapping="NoWrap"
                             FontFamily="Consolas"
                             FontSize="11"
                             Background="#1E1E1E"
                             Foreground="#DCDCDC"/>
                </ScrollViewer>
            </Grid>
        </Border>
        
        <!-- Status Bar -->
        <Border Grid.Row="4" Background="#007ACC" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Foreground="White" x:Name="txtCurrentStudent">
                    <Run Text="Current: "/>
                    <Run x:Name="runCurrentStudent" Text="-"/>
                </TextBlock>
                
                <TextBlock Grid.Column="1" Foreground="White" Margin="20,0">
                    <Run Text="Progress: "/>
                    <Run x:Name="runGraded" Text="0"/>
                    <Run Text="/"/>
                    <Run x:Name="runTotal" Text="0"/>
                    <Run Text=" ("/>
                    <Run x:Name="runPercent" Text="0"/>
                    <Run Text="%)"/>
                </TextBlock>
                
                <TextBlock Grid.Column="2" Foreground="#90EE90" Margin="20,0">
                    <Run Text="Success: "/>
                    <Run x:Name="runSuccess" Text="0"/>
                </TextBlock>
                
                <TextBlock Grid.Column="3" Foreground="#FFB6C1" Margin="20,0">
                    <Run Text="Failed: "/>
                    <Run x:Name="runFailed" Text="0"/>
                </TextBlock>
                
                <TextBlock Grid.Column="4" Foreground="#FFFACD" Margin="20,0">
                    <Run Text="Not Run: "/>
                    <Run x:Name="runNotRun" Text="0"/>
                </TextBlock>
                
                <TextBlock Grid.Column="5" Foreground="White" Margin="20,0,0,0">
                    <Run Text="Elapsed: "/>
                    <Run x:Name="runElapsed" Text="-"/>
                </TextBlock>
            </Grid>
        </Border>
    </Grid>
</Window>
